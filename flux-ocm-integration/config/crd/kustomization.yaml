# This kustomization.yaml is not intended to be run by itself,
# since it depends on service name and namespace that are out of this kustomize package.
# It should be run by config/default
resources:
- bases/transfer.phoban.io_componentsyncs.yaml
- bases/transfer.phoban.io_repositorycredentials.yaml
- bases/transfer.phoban.io_components.yaml
- bases/transfer.phoban.io_resources.yaml
- bases/transfer.phoban.io_transformations.yaml
- bases/transfer.phoban.io_transformationlocalstorages.yaml
- bases/transfer.phoban.io_ocmkustomizations.yaml
- bases/transfer.phoban.io_realizations.yaml
#+kubebuilder:scaffold:crdkustomizeresource

patchesStrategicMerge:
# [WEBHOOK] To enable webhook, uncomment all the sections with [WEBHOOK] prefix.
# patches here are for enabling the conversion webhook for each CRD
#- patches/webhook_in_componentsyncs.yaml
#- patches/webhook_in_repositorycredentials.yaml
#- patches/webhook_in_components.yaml
#- patches/webhook_in_resources.yaml
#- patches/webhook_in_transformations.yaml
#- patches/webhook_in_transformationlocalstorages.yaml
#- patches/webhook_in_ocmkustomizations.yaml
#- patches/webhook_in_realizations.yaml
#+kubebuilder:scaffold:crdkustomizewebhookpatch

# [CERTMANAGER] To enable cert-manager, uncomment all the sections with [CERTMANAGER] prefix.
# patches here are for enabling the CA injection for each CRD
#- patches/cainjection_in_componentsyncs.yaml
#- patches/cainjection_in_repositorycredentials.yaml
#- patches/cainjection_in_components.yaml
#- patches/cainjection_in_resources.yaml
#- patches/cainjection_in_transformations.yaml
#- patches/cainjection_in_transformationlocalstorages.yaml
#- patches/cainjection_in_ocmkustomizations.yaml
#- patches/cainjection_in_realizations.yaml
#+kubebuilder:scaffold:crdkustomizecainjectionpatch

# the following config is for teaching kustomize how to do kustomization for CRDs.
configurations:
- kustomizeconfig.yaml
